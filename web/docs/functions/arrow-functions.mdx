# Arrow Functions

Arrow functions provide a clean, concise syntax for creating anonymous functions. They are extremely useful for callbacks or storing functions in variables.
```rust
multiply = fn(a, b) => {
    rev a * b
}

pt(multiply(5, 4)) // 20
```
*Note: In DryLang, arrow functions behave exactly like standard functions, but omit the function name.*

