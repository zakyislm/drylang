# State & Jobs Overview

Managing state (caches, rate limiting) and background jobs (cron, delayed processing) typically requires Redis and external workers.

In dryLang, these features are **built directly into the runtime memory**, saving you from deploying extra infrastructure.
