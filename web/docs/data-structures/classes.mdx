# Classes

Use `cl` to define a class with fields and methods. Methods use `fn`.

```rust
cl Calculator {
    fn add(a, b) {
        rev a + b
    }
}

calc = Calculator()
pt(calc.add(100, 50)) // 150
```
