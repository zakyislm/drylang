# Comments

Use comments to explain your code. Comments are completely ignored by the DryLang parser.

## Single Line
Use `//` for a single line comment.
```rust
// This is a comment
let a = 10 // This is an inline comment
```
## Multi-line
(Coming soon or refer to specific implementation if `/* */` is supported).
